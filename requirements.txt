# Core deps cho pipeline qa (ReAct) + ehr (EhrAgent) + algo/ (tối ưu trigger).
# Agent-Driver cần thêm requirements-agentdriver.txt.
# torch cài riêng theo CUDA của máy, xem Makefile target `torch-cu121`.

torch
# Code targets the current Transformers API.  Keep the major version bounded so
# future breaking releases do not silently change the Kaggle environment.
transformers>=5,<6
tokenizers
huggingface-hub
accelerate
peft
datasets
sentence-transformers
safetensors

numpy
pandas
scikit-learn
matplotlib
seaborn

openai
tiktoken
replicate
ag2       # cung cấp module `autogen` cho EhrAgent
tenacity
requests
beautifulsoup4
google-search-results   # module `serpapi`, chỉ khi ReAct dùng live web search

braintrust          # tracing từng bước của agent (tuỳ chọn, bật bằng BRAINTRUST_API_KEY)
langfuse       # tracing cho src/toolpoison/ (tuỳ chọn, bật bằng LANGFUSE_PUBLIC_KEY/LANGFUSE_SECRET_KEY)
python-dotenv
jsonlines
ndjson
tqdm
termcolor
wandb
Levenshtein
gym
wolframalpha
