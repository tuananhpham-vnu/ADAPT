# Đường dẫn tương thích cho extension ADAPT

Implementation chuyển sang `src/adapt/extensions/`. Các import cũ vẫn hoạt động.
Xem [phân chia và trạng thái hiện tại](../adapt/README.md).
Đây là phần mở rộng của dự án. Mã gốc ARTEMIS chưa được vendor vào `src/artemis/`.
